# 部署到云服务器完整指南

## 🌟 方案对比

| 方案 | 难度 | 时间 | 费用 | 是否需要备案 | 推荐度 |
|------|------|------|------|-------------|--------|
| **Render.com** | ⭐ 简单 | 5分钟 | 免费 | ❌ 不需要 | ⭐⭐⭐⭐⭐ |
| **Railway.app** | ⭐ 简单 | 5分钟 | 免费 | ❌ 不需要 | ⭐⭐⭐⭐⭐ |
| 国外VPS | ⭐⭐ 中等 | 30分钟 | $5/月 | ❌ 不需要 | ⭐⭐⭐⭐ |
| 国内云服务器 | ⭐⭐⭐ 复杂 | 15-20天 | ¥50/月 | ✅ **必须** | ⭐⭐ |

---

## 🚀 方案1：Render.com 免费部署（最推荐）

### 优点
- ✅ 完全免费
- ✅ 无需备案
- ✅ 自动HTTPS
- ✅ 5分钟完成
- ✅ 自动重启
- ✅ 提供免费域名：`你的项目.onrender.com`

### 部署步骤

#### 第1步：注册GitHub账号
1. 访问 https://github.com
2. 注册账号（如果没有）

#### 第2步：上传代码到GitHub
```bash
# 在项目目录执行
cd /Users/hem/hemaj/workspace_vscode/WebServerTest

# 初始化Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "初始提交"

# 在GitHub创建新仓库，然后关联
git remote add origin https://github.com/你的用户名/WebServerTest.git
git branch -M main
git push -u origin main
```

#### 第3步：部署到Render
1. 访问 https://render.com
2. 用GitHub账号登录
3. 点击 "New +" → "Web Service"
4. 选择你的 `WebServerTest` 仓库
5. 配置如下：
   ```
   Name: webserver-test
   Environment: Node
   Build Command: npm install
   Start Command: npm start
   ```
6. 点击 "Create Web Service"
7. 等待3-5分钟，部署完成！

#### 第4步：访问
```
https://webserver-test.onrender.com
```

**就这么简单！** 🎉

---

## 🚀 方案2：Railway.app 免费部署

### 优点
- ✅ 完全免费（每月500小时）
- ✅ 更快的部署速度
- ✅ 更简单的配置

### 部署步骤

1. 访问 https://railway.app
2. 用GitHub登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择 `WebServerTest`
5. 自动检测Node.js项目，**无需任何配置**
6. 部署完成！

**访问地址**：`https://你的项目.up.railway.app`

---

## 🖥️ 方案3：国外VPS部署（推荐进阶用户）

### 推荐服务商
- **Vultr**：$5/月，日本机房速度快
- **DigitalOcean**：$6/月，稳定
- **Linode**：$5/月，可靠

### 完整部署步骤

#### 第1步：购买服务器
1. 注册 https://www.vultr.com
2. 选择服务器：
   - 地区：Tokyo（日本）或 Singapore（新加坡）
   - 操作系统：Ubuntu 22.04
   - 套餐：$5/月（1GB内存）
3. 获得服务器IP：如 `123.45.67.89`

#### 第2步：连接服务器
```bash
# macOS终端执行
ssh root@123.45.67.89
# 输入购买时收到的密码
```

#### 第3步：安装Node.js
```bash
# 更新系统
apt update && apt upgrade -y

# 安装Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 验证安装
node --version  # 应显示 v18.x.x
npm --version
```

#### 第4步：上传代码
```bash
# 方式1：用Git（推荐）
cd /root
git clone https://github.com/你的用户名/WebServerTest.git
cd WebServerTest
npm install

# 方式2：手动上传
# 在本地打包：tar -czf project.tar.gz WebServerTest/
# 用scp上传：scp project.tar.gz root@123.45.67.89:/root/
# 在服务器解压：tar -xzf project.tar.gz
```

#### 第5步：启动服务
```bash
# 临时启动（测试）
npm start

# 访问 http://123.45.67.89:3000 验证
```

#### 第6步：配置自动运行（使用PM2）
```bash
# 安装PM2
npm install -g pm2

# 启动应用
pm2 start server.js --name webserver-test

# 设置开机自启
pm2 startup
pm2 save

# 常用命令
pm2 status              # 查看状态
pm2 logs webserver-test # 查看日志
pm2 restart webserver-test  # 重启
pm2 stop webserver-test     # 停止
```

#### 第7步：配置Nginx反向代理（可选）
```bash
# 安装Nginx
apt install -y nginx

# 创建配置文件
nano /etc/nginx/sites-available/webserver

# 粘贴以下内容：
server {
    listen 80;
    server_name 123.45.67.89;  # 或你的域名

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# 启用配置
ln -s /etc/nginx/sites-available/webserver /etc/nginx/sites-enabled/
nginx -t  # 测试配置
systemctl restart nginx

# 现在可以通过 http://123.45.67.89 访问（不需要端口号）
```

#### 第8步：配置HTTPS（可选但推荐）
```bash
# 安装Certbot
apt install -y certbot python3-certbot-nginx

# 如果有域名（如 api.example.com）
certbot --nginx -d api.example.com

# 自动续期
certbot renew --dry-run
```

**总耗时**：约30分钟  
**费用**：$5/月（约¥35）

---

## 🇨🇳 方案4：国内云服务器部署（需要备案）

### 主要服务商
- 阿里云：https://www.aliyun.com
- 腾讯云：https://cloud.tencent.com
- 华为云：https://www.huaweicloud.com

### ⚠️ 备案流程（必须）

#### 为什么需要备案？
- 国内法律要求：所有公网服务必须备案
- 未备案无法使用80/443端口
- 未备案域名无法访问

#### 备案步骤
1. **购买服务器**（需要≥3个月）
2. **准备材料**：
   - 身份证正反面照片
   - 手持身份证照片
   - 网站负责人照片
   - 域名证书
   - 单位还需营业执照
3. **提交备案**：
   - 在云服务商控制台提交
   - 填写网站信息
   - 上传证件照片
4. **等待审核**：
   - 云服务商初审：1-2天
   - 管局审核：10-20天
5. **备案成功**：
   - 获得备案号（如：京ICP备12345678号）
   - 需在网站底部显示备案号

**总耗时**：15-20天  
**费用**：服务器¥50-100/月 + 域名¥50/年

### 技术部署步骤
（与方案3相同，参考上面的VPS部署步骤）

---

## 🆓 方案5：其他免费平台

### Fly.io
```bash
# 安装CLI
curl -L https://fly.io/install.sh | sh

# 登录
fly auth login

# 部署
fly launch
fly deploy
```

### Cyclic.sh
- 访问 https://cyclic.sh
- 连接GitHub
- 选择仓库，自动部署

### Vercel（需改造为Serverless）
- 适合静态网站 + API路由
- 需要修改代码结构

---

## 📊 费用对比

| 方案 | 服务器 | 域名 | 备案 | 总计/年 |
|------|--------|------|------|---------|
| Render.com | 免费 | 免费子域名 | 不需要 | **¥0** |
| Railway | 免费 | 免费子域名 | 不需要 | **¥0** |
| Vultr | $60/年 | $10/年 | 不需要 | **¥500** |
| 阿里云 | ¥600/年 | ¥50/年 | ¥0 | **¥650** |

---

## 🎯 我的推荐

### **学习/测试阶段**：
→ **Render.com** 或 **Railway**
- 理由：免费、简单、5分钟部署

### **个人项目/小流量**：
→ **Vultr（日本机房）**
- 理由：$5/月、速度快、无需备案

### **商业项目/大流量/国内用户**：
→ **阿里云 + 备案**
- 理由：速度最快、稳定、正规

---

## 🔒 安全建议

### 1. 环境变量配置
```bash
# 创建 .env 文件（不要提交到Git）
PORT=3000
NODE_ENV=production

# 在代码中使用
const PORT = process.env.PORT || 3000;
```

### 2. 添加 .gitignore
```
node_modules/
uploads/
.env
*.log
.DS_Store
```

### 3. 配置防火墙（VPS）
```bash
# 只开放必要端口
ufw allow 22    # SSH
ufw allow 80    # HTTP
ufw allow 443   # HTTPS
ufw enable
```

### 4. 限制上传文件大小（已配置）
```javascript
limits: { fileSize: 10 * 1024 * 1024 } // 10MB
```

---

## 🆘 常见问题

### Q1: 国外服务器慢吗？
**A**: 选日本/新加坡机房，延迟100-150ms，可接受。Render免费版可能较慢。

### Q2: 备案很麻烦吗？
**A**: 是的，需要15-20天，准备多份材料。新手不推荐。

### Q3: 免费平台有限制吗？
**A**: 
- Render：睡眠机制（15分钟无访问会休眠，首次访问需等待）
- Railway：每月500小时（约20天）
- 文件上传不持久（重启丢失）

### Q4: 如何持久化存储文件？
**A**: 
- 方案1：使用云存储（阿里云OSS、AWS S3）
- 方案2：使用数据库存储文件路径
- 方案3：VPS自带硬盘

### Q5: 需要购买域名吗？
**A**: 
- 免费平台：自带域名，不需要购买
- VPS：可用IP访问，域名可选
- 国内备案：必须有域名

---

## 🚀 快速开始（5分钟）

```bash
# 1. 推送代码到GitHub
git init
git add .
git commit -m "部署准备"
git remote add origin https://github.com/你的用户名/WebServerTest.git
git push -u origin main

# 2. 访问 render.com，用GitHub登录
# 3. 点击部署，等待5分钟
# 4. 完成！
```

---

## 📚 需要帮助？

我可以帮你：
1. ✅ 设置GitHub仓库
2. ✅ 配置Render部署
3. ✅ 购买VPS并配置
4. ✅ 解决部署问题

**你想用哪个方案？我来指导你完成！** 😊
